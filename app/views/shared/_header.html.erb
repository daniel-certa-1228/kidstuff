<nav class="navbar navbar-expand-lg navbar-dark bg-blue">
  <%= link_to 'Kid Stuff', root_path, class: "navbar-brand kid-font" %>
  <% if logged_in? %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <%= link_to 'Home', root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Add a Child', new_child_path, {:class => "nav-link"} %>
          </li>
          <li class="nav-item">
            <%= link_to 'See All Children', children_path, {:class => "nav-link"} %>
          </li>
          <li class="nav-item">
            <%= link_to 'User Profile', user_path(@current_user.id), {:class => "nav-link"} %>
          </li>
          <li class="nav-item">
            <%= link_to 'Logout', logout_path, {:class => "nav-link"} %>
          </li>
        </ul>
      </div>
  <% end %>
</nav>